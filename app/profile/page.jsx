import React from "react";
import ProfileCard from "../profileCard";

const Page = () => {
  return <div>
    <ProfileCard />
    
    </div>;
};

export const metadata = {
  title: "Profile",
  description: "This is the profile page of Todo App",
};

export default Page;
